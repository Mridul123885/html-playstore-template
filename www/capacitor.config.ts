import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mridul.myapp',
  appName: 'My HTML App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
